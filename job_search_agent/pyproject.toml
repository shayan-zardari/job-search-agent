[project]
name = "job_search_agent"
version = "0.1.0"
description = "job-search-agent using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.9.3"
]

[project.scripts]
job_search_agent = "job_search_agent.main:run"
run_crew = "job_search_agent.main:run"
train = "job_search_agent.main:train"
replay = "job_search_agent.main:replay"
test = "job_search_agent.main:test"
run_with_trigger = "job_search_agent.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
